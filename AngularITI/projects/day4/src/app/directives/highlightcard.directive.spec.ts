import { HighlightcardDirective } from './highlightcard.directive';

describe('HighlightcardDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightcardDirective();
    expect(directive).toBeTruthy();
  });
});
